[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rohithtp-cv"
version = "0.1.0"
description = "CV project with AI-assisted development setup"
authors = [
    {name = "Rohith TP", email = "rohithtp@gmail.com"}
]
requires-python = ">=3.8"
dependencies = [
    "aider-chat"
]

[tool.setuptools]
packages = []